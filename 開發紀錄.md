# twse_multi_agent 開發紀錄

> 專案路徑：`/home/sweet/.gemini/antigravity/scratch/twse_multi_agent`  
> GitHub：https://github.com/ergeargwer/twse_multi_agent  
> 入口 dashboard 專案 id：`twse_multi_agent`（Streamlit 埠 **8505**）  
> 本檔更新：2026-08-14

本文保存決策背景、里程碑、已知限制與測試狀態，供後續維護與交接。  
對話記憶見 [MEMORY_LOG.md](./MEMORY_LOG.md)；對外說明見 [README.md](./README.md)。

---

## 1. 專案定位

為台灣股市（TWSE / TPEx）打造的多 Agent **分析**架構（PoC／MVP），不是自動交易系統。

硬性原則：

- Phase 2 Agent **Stateless**、彼此不可見，只讀 Shared Context 自己需要的欄位
- 禁止給買賣點位與「應該買／應該賣」
- 與券商只做**唯讀**查詢，不封裝下單
- 開放來源（新聞／公告）一律標 `unverified`，即使雙 CLI 交叉也不得當事實

---

## 2. 時間線總覽

| 階段 | 日期 | 重點 | 參考 commit |
|------|------|------|-------------|
| 0. 架構重構 MVP | 2026-04 | SharedContext、Pipeline、四個 Base Agent | `f45fa2b` |
| 1. FinMind + LLM + Trace | 2026-04 | 真實價量／籌碼、OpenRouter、trace 觀測 | `f6dde12` `3eb0f35` |
| 2. Shioaji 與輔助人格 | 2026-04 | 帳戶唯讀、資產配置／定價／風控／紀律、Streamlit | `17c1f76` |
| 3. Fail-safe 否決 | 2026-07 | 拿掉假資料 fallback；帳戶不可用預設否決 | `a40d4f8` |
| 4. 行為風險整合 | 2026-08-14 | 移植 `stock_risk_alert` 為 Phase 2 Agent + Plotly | 本檔階段 4 |
| 5. 雙 CLI 合作蒐集 | 2026-08-14 | Grok Build CLI + Gemini CLI 平行蒐集未驗證事件 | 本檔階段 5 |
| 6. 文件與發布 | 2026-08-14 | 開發紀錄、README、修正 `src/trace/` 被 gitignore 誤傷 | `b44a007` |
| 7. 規則與 Prompt 外部化 | 2026-08-14 | `config/rules.yaml`、人設模板、系統設定唯讀分頁 | 分支 `feature/config-externalization` |

---

## 3. 階段 0–3（既有主線，摘要）

### 3.1 三階段流水線

1. **Phase 1 Ingestion**：FinMind（價量、法人、財報、行事曆）+ Shioaji（帳戶／庫存，可缺）
2. **Phase 2 平行盲測**：基本面、技術面、法人籌碼、制度事件，以及資產配置、定價把關、風控煞車、執行紀律
3. **Phase 3 Synthesizer**：匯整報告交 LLM 做多空矛盾與情境推演；程式附風暴比與分批提醒

### 3.2 Fail-safe（2026-07）

帳戶金鑰未設或查詢失敗時，風控煞車 **veto=True**，不回傳假帳戶數字。UI 會區分「帳戶不可用攔截」與「個股訊號攔截」。

### 3.3 LLM 後備鏈

`DecisionSynthesizerAgent` 依金鑰順序嘗試 SpaceXAI（`XAI_API_KEY`）→ OpenRouter → Gemini。可用 `LLM_PROVIDER` 指定偏好。

---

## 4. 階段 4：行為風險整合（2026-08-14）

來源專案：`/home/sweet/.gemini/antigravity/scratch/stock_risk_alert`。

| 決策 | 選擇 | 理由 |
|------|------|------|
| 掛載位置 | 新 Phase 2 Agent，不併入 Technical | 遵守盲測：行為規則與均線／KD 訊號隔離 |
| 資料 | 既有日線 `price_action.raw_history` | 不另打 FinMind 分鐘線，避免雙源不一致與額度 |
| 否決 | 不觸發 Risk Veto | 只做觀察標記，不當自動煞車 |
| 視覺化 | `src/ui/risk_chart.py` Plotly | 保留原專案 K 線三角形，配色對齊儀表板 |

規則（與來源專案相同，同一根 K 線高追價優先）：

- **高追價**：VWAP 乖離、創新高波動急縮、價漲量縮
- **低殺出**：跌破支撐無量、假跌破、低檔盤旋

新增檔案：

- `src/analysis/behavior_risk.py`
- `src/agents/behavior_risk.py`
- `src/ui/risk_chart.py`
- `scratch/test_behavior_risk.py`

UI：個股分析報告可設 VWAP 閾值；新增「行為風險」分頁，可調閾值重算或只抓價量（不跑 LLM）。

測試：`./venv/bin/python scratch/test_behavior_risk.py`（空資料／高追價／低殺出／Phase 2 執行緒寫入）。

---

## 5. 階段 5：雙 CLI 合作蒐集（2026-08-14）

需求：用 Grok Build CLI 與 Gemini CLI **同時**補 API 沒有的新聞／公告；兩邊皆有高額度。

| 決策 | 選擇 | 理由 |
|------|------|------|
| 掛載位置 | Phase 1 背景平行，不進 Phase 2 | CLI 非決定性，不能污染盲測 Agent |
| 互叫 | 禁止；由 Python 編排兩邊 | 避免套娃、搶 session、難除錯 |
| 工具範圍 | 只准搜尋／抓頁 | 蒐集不得改檔、開 shell |
| 工作目錄 | 各自 `TemporaryDirectory` | 避開 `~/.grok/active_sessions.lock` 與本層 Grok TUI |
| 失敗策略 | 單邊或雙邊失敗回空陣列 | 不擋 FinMind／Shioaji |
| 證據強度 | 一律 `unverified`；URL／標題去重後標 `both` | 交叉只是對照，不是事實 |

實作要點：

- 模組：`src/integrations/cli_collectors.py`
- Grok：`grok -p`、`--json-schema`、`--tools web_search,web_fetch`、讀 `structuredOutput`
- Gemini：必須用 **Node 22** 啟動套件（系統 PATH 的 Node 18 會 `Invalid regular expression flags`）；`-o json`、`--approval-mode plan`、`--skip-trust`；讀 `response` 字串
- Event Agent 產出 `open_source_signal`：`雙源交叉印證`／`單源待查`／`開放來源未產出`
- Synthesizer 明確禁止把開放來源當既定事實

環境變數：

| 變數 | 預設 | 用途 |
|------|------|------|
| `CLI_COLLECT_ENABLED` | `1` | `0` 關閉整段 |
| `CLI_COLLECT_TIMEOUT` | `180` | 單邊秒數 |
| `CLI_COLLECT_MAX_TURNS` | `8` | Grok 回合上限 |
| `GROK_CLI_BIN` | `~/.local/bin/grok` | 可覆寫 |
| `GEMINI_CLI_BIN` / `GEMINI_CLI_NODE` | Node 22 + gemini.js | 可覆寫 |

測試：`./venv/bin/python scratch/test_cli_collectors.py`（解析、去重、Event Agent）。  
煙霧：隔離目錄短指令確認兩支 CLI 都能非互動回 JSON。完整新聞搜尋留給實盤 Pipeline。

---

## 6. 階段 6：文件與發布（2026-08-14）

- 新增本檔；重寫 README（真實 clone URL、目錄、環境變數、入口埠、測試）
- 更新 MEMORY_LOG（刪除已過時的「下一步：接 LLM」）
- **修正 `.gitignore`**：原 `trace/` 連 `src/trace/` 原始碼一併忽略，導致 Collector／Visualizer 從未進版控。改為只忽略根目錄 `/trace/`（執行期 dump）
- 補 `requirements.txt`
- 入口網 `dashboard.py` 卡片說明已含行為風險與雙 CLI

---

## 6.1 階段 7：規則參數與 Prompt 外部化（2026-08-14）

門檻與「樹之修行者」人設改由設定檔載入，**判斷邏輯與數值不變**，本階段不做 UI 編輯套用。

- `config/rules.yaml` + `src/core/rule_config.py`：九個 Phase 2 Agent 讀 `self.rules`
- `config/prompts/synthesizer_persona_v1.yaml` + `src/core/prompt_config.py`
- Trace 與 Synthesizer 報告附 `rule_config_version` / `prompt_persona_version`
- UI「系統設定」分頁唯讀展示並可下載原始檔
- 缺檔或缺區塊直接拋錯，不靜默 fallback
- 測試：`scratch/test_rule_config.py`

---

## 7. 目前架構（發布時）

```text
Phase 1  DataIngestion + 雙 CLI 蒐集
         └─ ingested_data（含 open_source_events）
Phase 2  九個平行 Agent（互不可見）
         基本 / 技術 / 籌碼 / 制度 / 資產配置 / 定價 / 風控 / 紀律 / 行為風險
Phase 3  Synthesizer + LLM 後備鏈
```

入口：Antigravity Portal `http://127.0.0.1:8888` → 啟動後 Streamlit `http://127.0.0.1:8505`  
指令：`PYTHONPATH=. venv/bin/streamlit run src/ui/app.py --server.port 8505`

---

## 8. 測試狀態

| 腳本 | 範圍 | 狀態 |
|------|------|------|
| `scratch/test_behavior_risk.py` | 行為風險規則與 Agent 寫入 | 2026-08-14 通過 |
| `scratch/test_cli_collectors.py` | JSON 解析、合併去重、Event Agent | 2026-08-14 通過 |
| `scratch/test_fail_safe.py` | 帳戶缺金鑰／失敗否決 | 既有，本次未重跑 |
| `scratch/test_shioaji.py` | 券商連線 | 需真實金鑰 |

未做：完整 FinMind + 雙 CLI 搜尋 + LLM 的端到端實盤（耗時與額度；蒐集層已用短指令驗證 CLI 可啟動）。

---

## 9. 已知限制

- 行事曆欄位（融券回補、ETF 觀察名單、融資維持率、庫藏股）仍常為「資料源待補」；雙 CLI 只補非結構化線索
- Gemini CLI 綁 Node ≥ 20；本機預設 `env node` 是 v18，必須走 nvm Node 22
- 從正在跑的 Grok TUI 再 `grok -p` 可能搶 session；實作已用獨立 cwd 並清掉 `GROK_AGENT`
- `src/trace/` 過去未進 Git；clone 舊版的人缺該套件，本次發布後才完整
- 行為風險僅日線；原 `stock_risk_alert` 的 5 分線未併入
- 系統不下單

---

## 10. 後續可做

- 新聞情緒獨立 Agent（仍只讀 `open_source_events`，不要自己開 CLI）
- 行事曆改接更穩定的除權息／停券來源
- Threading 升級 asyncio
- `main.py` 標的改由 CLI 參數／`.env` 指定（UI 已可輸入）
